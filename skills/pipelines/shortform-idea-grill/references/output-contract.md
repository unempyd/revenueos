# Short-form output contract

Return this exact visible table unless the user requests different columns:

| Rank | Topic | Five-second overlay / hook | Virality /10 | 3-sec hook /10 | Three yap bullets | Complete payoff / proof | CTA |
|---:|---|---|---:|---:|---|---|---|

Rules:

- Rank from strongest recording priority to weakest.
- Use one hook, not a list of alternates.
- Keep the overlay understandable without audio and visible for about five seconds.
- Put exactly three numbered talking bullets in each row, separated with `<br>`.
- Make the payoff resolve the literal hook promise.
- Link the real source or proof artifact in the payoff cell when one exists.
- Show the CTA as `KEYWORD → destination` when a destination exists.
- Do not add predicted views.
- Do not add extra columns unless requested.

After the table, add only a one-line recording-order recommendation when useful.
