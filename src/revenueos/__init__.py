"""RevenueOS — a plug-in autonomous revenue department assembled from open-source parts.

Discover → qualify → create → publish → advertise → engage → follow up → measure → optimize.
The customer sees one questionnaire and one daily brief (`revenueos today`); everything
else is workers built on vendored code (see VENDOR.json) driven by the orchestrator.
"""

__version__ = "0.1.1"
